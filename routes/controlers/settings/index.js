const { get } = require("./get");
// const { post } = require("./post");
const { update } = require("./update");
// const { remove } = require("./remove");

const settingsControler = { get, update };

module.exports = settingsControler;
